{
  "name": "@repo/organization-user-service",
  "type": "module",
  "scripts": {
    "test": "vitest",
    "dev": "wrangler dev --host 0.0.0.0 --port 6173 --inspector-port 7173",
    "build": "wrangler types --env-interface CloudflareBindings",
    "deploy:release": "pnpm exec env-config-materialize --env release && wrangler deploy --config .generated/wrangler.release.jsonc --minify",
    "deploy:staging": "pnpm exec env-config-materialize --env staging && wrangler deploy --config .generated/wrangler.staging.jsonc --minify",
    "deploy:preview": "pnpm exec env-config-materialize --env preview && wrangler deploy --config .generated/wrangler.preview.jsonc --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings",
    "check-types": "tsc --noEmit",
    "lint": "eslint ."
  },
  "dependencies": {
    "hono": "^4.13.5"
  },
  "devDependencies": {
    "@repo/env-config": "workspace:*",
    "@cloudflare/workers-types": "^5.20260902.1",
    "@eslint/js": "^10.0.1",
    "eslint": "^10.9.0",
    "globals": "^17.11.0",
    "typescript": "~6.0.2",
    "typescript-eslint": "^8.69.0",
    "vitest": "^4.1.11",
    "wrangler": "^4.110.0"
  }
}
