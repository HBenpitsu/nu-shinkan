name: Workerターゲットの解決
description: configs-cli を使用してWorkerパッケージのメタデータターゲットを解決します。
inputs:
  packages:
    description: パッケージ名のJSON配列
    required: true
outputs:
  targets:
    description: JSON形式のWorkerターゲットメタデータ
runs:
  using: node24
  main: resolve-targets.mjs
