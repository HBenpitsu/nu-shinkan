name: Pick deploy
description: Select, test, build and deploy the pick flow in a fixed checkout.
inputs:
  channel:
    description: Deployment channel
    default: "preview"
  head_sha:
    description: Fixed target SHA
    required: true
  base_sha:
    description: Comparison SHA
    default: ""
  pr_number:
    description: Preview PR number
    default: "-1"
  picks:
    description: Exact package names as JSON
    default: "[]"
outputs:
  changes:
    description: Test packages
    value: ${{ steps.plan.outputs.changes }}
  results:
    description: Deployment results
    value: ${{ steps.deploy.outputs.results }}
runs:
  using: composite
  steps:
    - name: Bootstrap root dependencies
      uses: ./.github/actions/use-repo
      with:
        filter: .
    - name: Validate picked package names
      shell: bash
      env:
        PICKS: ${{ inputs.picks }}
      run: node .github/actions/pick-deploy/check.mjs
    - name: Install dependencies
      uses: ./.github/actions/use-repo
    - name: Compile configuration tools
      shell: bash
      run: pnpm --filter @repo/app-config build
    - name: Select packages
      id: plan
      shell: bash
      env:
        HEAD_SHA: ${{ inputs.head_sha }}
        BASE_SHA: ${{ inputs.base_sha }}
        PICKS: ${{ inputs.picks }}
      run: pnpm exec tsx .github/actions/pick-deploy/plan.mjs
    - name: Sync local configuration
      shell: bash
      run: pnpm sync
    - name: Test changed packages
      shell: bash
      env:
        PACKAGES: ${{ steps.plan.outputs.changes }}
      run: node .github/scripts/task.mjs test
    - name: Build deployment targets
      shell: bash
      env:
        DEPLOY_CHANNEL: ${{ inputs.channel }}
        PR_NUMBER: ${{ inputs.pr_number }}
        TARGETS: ${{ steps.plan.outputs.targets }}
      run: |
        export PACKAGES=$(node -e 'console.log(JSON.stringify(JSON.parse(process.env.TARGETS).map(p => p.package)))')
        node .github/scripts/task.mjs build
    - name: Recheck PR before deployment
      id: state
      shell: bash
      env:
        PR_NUMBER: ${{ inputs.pr_number }}
      run: node .github/scripts/pr-state.mjs
    - name: Clean preview closed during preparation
      if: ${{ steps.state.outputs.operation == 'cleanup' }}
      uses: ./.github/actions/preview-prune
      with:
        pr_number: ${{ inputs.pr_number }}
    - name: Deploy prepared targets
      id: deploy
      if: ${{ steps.state.outputs.operation == 'deploy' }}
      shell: bash
      env:
        DEPLOY_CHANNEL: ${{ inputs.channel }}
        PR_NUMBER: ${{ inputs.pr_number }}
        TARGETS: ${{ steps.plan.outputs.targets }}
      run: pnpm exec tsx .github/scripts/deploy.mjs
