name: Comment failure reaction
description: Register the failure reaction at the end of the job.
inputs:
  token:
    description: GitHub token
    required: true
runs:
  using: node24
  main: main.mjs
  post: post.mjs
  post-if: failure()
