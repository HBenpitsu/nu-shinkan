name: Deploy Staging Tag

on:
  push:
    tags:
      - staging

jobs:
  deploy:
    uses: ./.github/workflows/deploy-tag.yml
    secrets: inherit
    with:
      base_sha: ${{ github.event.before }}
      tag_name: staging