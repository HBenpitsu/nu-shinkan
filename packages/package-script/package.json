{
  "name": "@repo/package-script",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -b",
    "prepare": "pnpm build",
    "bootstrap": "pnpm install --filter . && pnpm build",
    "test": "vitest src/",
    "lint": "eslint . --fix",
    "compile": "pnpm lint && pnpm test --run && pnpm build"
  },
  "bin": {
    "delete-preview-worker": "bin/delete-preview-worker.js"
  },
  "devDependencies": {
    "@repo/app-config": "workspace:*",
    "@eslint/js": "^10.0.1",
    "eslint": "^10.9.0",
    "globals": "^17.11.0",
    "jsonc-parser": "^3.3.1",
    "typescript": "~6.0.2",
    "typescript-eslint": "^8.69.0",
    "vitest": "^4.1.11"
  }
}
