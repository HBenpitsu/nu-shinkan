# PREMISE

You should scan `docs/*` and read documents relevant to your task to grasp project's premise.

When you step into some workspaces (directories in `packages/` or `apps/`) you should check if there is an `AGENT.md` in that directory. When you find `AGENT.md`, just check it.
